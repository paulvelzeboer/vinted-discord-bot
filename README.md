# Vinted Discord BOT


Pre :

* Node.js
* NPM

Installation :

* `npm install`
* `config.sample.json` -> `config.json`
* `node index.js`
* ore create docker container with docker file and run container
